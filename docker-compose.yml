services:
  picowatch:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - picowatch-data:/data
    environment:
      - DB_PATH=/data/picowatch.db
      - PORT=8080
    restart: unless-stopped

volumes:
  picowatch-data:
